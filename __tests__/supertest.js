const request = require('supertest');

describe('Route integration', () => {
  it('asdf', () => {
    return expect(1).toEqual(1);
  });
  it('asdf', () => {
    return expect(1).toEqual(1);
  });
});

describe('Rosdf', () => {
  it('asdf', () => {
    return expect(1).toEqual(1);
  });
});
