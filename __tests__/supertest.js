const request = require('supertest');

describe('Auth endpoints', () => {
  it('GET /auth/user', () => {
    return expect(1).toEqual(1);
  });
});

describe('Rosdf', () => {
  it('asdf', () => {
    return expect(1).toEqual(1);
  });
});
